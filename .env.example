# Rooney Capital runtime environment template
# -------------------------------------------
# Copy this file to `.env` and replace the placeholder values before running
# the live worker. Secrets should never be committed to source control.

# ---------------------------------------------------------------------------
# Databento Market Data
# ---------------------------------------------------------------------------
# Required. Generate a key from https://databento.com/account/api-keys and
# ensure it has access to the datasets referenced in your runtime configuration.
DATABENTO_API_KEY=your_databento_key_here

# ---------------------------------------------------------------------------
# TradersPost (webhook only - no REST API access is required)
# ---------------------------------------------------------------------------
# Required if you want the worker to emit order and trade notifications. Create
# a webhook in TradersPost, copy the URL, and paste it below. Leave blank to
# disable outbound notifications.
TRADERSPOST_WEBHOOK_URL=https://your-traderspost-webhook

# ---------------------------------------------------------------------------
# Safety Controls
# ---------------------------------------------------------------------------
# When true the runtime configuration starts in a "dry run" mode that blocks
# order submission. Set to "false" only after verifying connectivity, ML model
# hydration, and broker risk checks.
POLICY_KILLSWITCH=true

# ---------------------------------------------------------------------------
# Optional Overrides
# ---------------------------------------------------------------------------
# Commission per side overrides the value defined in src/config.py. Provide a
# broker-specific cost if it differs from the documented default (USD).
# PINE_COMMISSION_PER_SIDE=1.25

# Pair map override allows you to point a symbol to a different hedge/confirming
# contract. The value can be a JSON object or comma-delimited key:value pairs.
# PINE_PAIR_MAP={"ES": "NQ", "NQ": "ES"}

# Override the runtime configuration path if you keep configs outside the repo.
# PINE_RUNTIME_CONFIG=/opt/pine/runtime/config.yml

# Configure the heartbeat file path and write interval when running under a
# supervisor (systemd, Kubernetes, etc.) for external health checks.
# PINE_HEARTBEAT_FILE=/var/run/pine/worker_heartbeat.json
# PINE_HEARTBEAT_WRITE_INTERVAL=30
